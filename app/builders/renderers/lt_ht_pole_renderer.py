"""LT with HT/PSCC Pole renderer — same text as LT Standard per spec."""

from __future__ import annotations

from typing import Optional

from app.builders.renderers.lt_standard_renderer import LTStandardRenderer


class LTHTPoleRenderer(LTStandardRenderer):
    """Text blocks for LT-HT work type. Identical to LT Standard per Part 8."""

    pass
